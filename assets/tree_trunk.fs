#version 330
// Not sure if I want this script to be separate from the tree_tendril.fs

in vec2 fragTexCoord;

out vec4 finalColor;

void main() {
	// It's supposed to be a circle
	finalColor = vec4(1, 1, 0, 1);
}